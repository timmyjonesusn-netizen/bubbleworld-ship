name: Facebook Autoposter

on:
  workflow_dispatch: {}
  schedule:
    # Times are in UTC. Adjust as you like.
    # 08:00 & 22:00 UTC ≈ 2am & 4pm US Central (non-DST close).
    - cron: "0 8 * * *"
    - cron: "0 22 * * *"

permissions:
  contents: write    # to commit/move sent posts
  actions: read

jobs:
  post:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install deps
        run: |
          python -m pip install --upgrade pip

      - name: Post next item in queue
        env:
          FB_TOKEN_RAGLAND_PAGE: ${{ secrets.FB_TOKEN_RAGLAND_PAGE }}
          FB_TOKEN_TIMMYART_PAGE: ${{ secrets.FB_TOKEN_TIMMYART_PAGE }}
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          python post_to_fb.py
